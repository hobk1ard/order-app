import { FilterOwnerPipe } from './filter-owner.pipe';

describe('FilterOwnerPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOwnerPipe();
    expect(pipe).toBeTruthy();
  });
});
